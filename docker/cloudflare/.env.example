# Cloudflare Tunnel hostname for PostgreSQL replication
#
# Each server needs a tunnel to connect to the OTHER server's PostgreSQL.
# The hostname should be your Cloudflare tunnel hostname that points to
# the remote server's PostgreSQL port.
#
# Example topology:
#   PRIMARY (server-a.example.com) -> connects to STANDBY via pg-standby.example.com
#   STANDBY (server-b.example.com) -> connects to PRIMARY via pg-primary.example.com
#
# On PRIMARY (connects TO standby):
CF_PG_HOSTNAME=pg-standby.example.com

# On STANDBY (connects TO primary):
# CF_PG_HOSTNAME=pg-primary.example.com
